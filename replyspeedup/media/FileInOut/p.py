def xytoid(xy, w, h):
    return xy[1] * w + xy[0]

def idtoxy(id, w):
    return [id % w, id // w]


in_lines = """"""

for t in range(int(in_lines.pop())):
    out_line = ""

    out_lines.append(f"Case #{str(t+1)}: {out_line}\n")

# dont delete this line