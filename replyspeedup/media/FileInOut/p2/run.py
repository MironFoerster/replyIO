def key(s):    return s[0]n_cases = int(in_lines.pop())for t in range(n_cases):    N, B, b = in_lines.pop().split(" ")    N = int(N)    b =int(b)    B = int(B)    S = {}    for n in range(N):        c, r = in_lines.pop().split(" ")        c = int(c)        r = int(r)        d = r-c        if not d in S.keys():            S[d] = c        elif S[d] > c:            S[d] = c    keys = list(S.keys())    keys.sort()    keys = list(reversed(keys))    print(keys)    line = 0    print(b, B, S)    while b<B:        line +=1        plus = 0        for k in keys:            plus = k            if S[k]<=b:                break;                    b += plus    out_lines.append("Case #"+str(t+1)+": "+str(line)+"\n")